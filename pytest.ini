[pytest]
testpaths = tests
# Prevent automatic collection of the Demesne example integration tests
# They live under tests/demesne and can be run explicitly when desired.
norecursedirs = tests/demesne
