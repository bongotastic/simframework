# simframework

Minimal Python simulation framework starter.

Usage

Install deps (recommended in venv):

```bash
python -m pip install -r requirements.txt
```

Run demo:

```bash
python scripts/run_sim.py
```

Run tests:

```bash
pytest
```
